export interface Mixed {
	x: number;
	a: number;
	c: number;
	m: number;
}

export interface GeneratedMixed {
	initValues: Mixed;
	r: number[];
	areUniform: boolean;
}

export interface Normalize {
	r: number[];
	m: number;
}
